FROM python:3.12-alpine

WORKDIR /app
COPY app.py .

# Приложение слушает порт 8181
EXPOSE 8181

# Запускаем сервер, принудительно биндимся на 0.0.0.0, чтобы был доступен снаружи контейнера
CMD ["python", "-c", "from app import run; run('0.0.0.0', 8181)"]